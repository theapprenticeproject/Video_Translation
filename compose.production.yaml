# Production compose — run with:
#   docker compose -f compose.production.yaml up --build -d

services:
  redis:
    ports: []

  backend:
    ports: []
  
  frontend:
    profiles:
      - dev-only

  nginx:
    build:
      context: ./client
      dockerfile: Dockerfile
      target: prod         # ← multi-stage: builds React app, copies dist/ into nginx image
      args:
        # Empty = relative URLs; nginx proxies /api → backend:8000 internally
        VITE_API_SERVER_URL: ""
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/prod.conf:/etc/nginx/conf.d/default.conf:ro
      # SSL certs — mount from host (e.g. Certbot/Let's Encrypt):
      - /etc/letsencrypt:/etc/letsencrypt:ro
    networks:
      - app-network
    depends_on:
      backend:
        condition: service_healthy

networks:
  app-network:
    driver: bridge
